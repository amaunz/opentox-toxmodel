function getsmiles() {
  if (document.JME.smiles() != '') {
    $('smiles').value = document.JME.smiles() ;
  }
}

function getsmarts() {
  if (document.JME.smiles() != '') {
    $('smarts').value = document.JME.smiles() ;
  }
}
